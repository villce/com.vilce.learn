<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vilce.springboot_vue.module.user.mapper.AdminMenuMapper">

    <select id="getMenuById" resultType="com.vilce.springboot_vue.module.user.model.po.AdminMenu" >
		SELECT *
		FROM admin_menu
		WHERE id = #{mid}
    </select>

    <select id="findAllByParentId" resultType="com.vilce.springboot_vue.module.user.model.po.AdminMenu" >
		SELECT *
		FROM admin_menu
		WHERE parent_id = #{parent_id}
    </select>

</mapper>
